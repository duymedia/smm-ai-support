# Database Connection (MySQL: user=root, pass=empty, database=rent_ai_suport)
DATABASE_URL="mysql://root:@127.0.0.1:3306/smm_panel"

# JWT Security
JWT_SECRET="nexussmm_super_secure_jwt_token_secret_key_2026"
JWT_EXPIRES_IN="7d"

# Upstash Redis Rate Limiting (Optional - Falls back to in-memory if empty)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# RabbitMQ Message Broker
RABBITMQ_URL="amqp://localhost:5672"

